import styled from 'styled-components';

export const PieChartDemoContainer = styled.div`
  .pie-chart-container {
    width: clamp(150px, 100%, 220px);
    height: clamp(150px, 100%, 220px);
  }
`;

export const InsideLabelDemoContainer = styled.div``;
