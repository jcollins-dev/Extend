import React from 'react';
import { SiteFooterContainer } from './SiteFooter.elements';

export const SiteFooter = (): JSX.Element => {
  return (
    <SiteFooterContainer className="site-layout__footer">
      Site Footer Placeholder
    </SiteFooterContainer>
  );
};
