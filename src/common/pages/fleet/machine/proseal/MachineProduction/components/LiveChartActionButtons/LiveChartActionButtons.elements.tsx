import styled from 'styled-components';

export const LiveChartActionButtonsContainer = styled.div`
  display: flex;
  flex-direction: row;

  .livechart--button {
    display: flex;
    flex-direction: row;
    background: none;
    border: 0;
    cursor: pointer;

    svg {
      margin-right: 0.5rem;
    }
  }
`;
